apiVersion: extensions/v1beta1
kind: Ingress

metadata:
  name: {{ template "fullname" . }}
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"

spec:
  rules:
  - host: "gerrit.{{ .Values.domain }}"
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ template "fullname" . }}
          servicePort: 8080

  tls:
    - secretName:  "tls-gerrit.{{ .Values.domain }}"
      hosts:
        - "gerrit.{{ .Values.domain }}"
